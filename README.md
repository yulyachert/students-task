#### Интерфейс для работы со списком студентов

Настройка базы данных в `index.ts`

Настройка портов в `Server.ts`

Запуск приложения
```
    cd server
    npm install
    npm run start:dev
    cd client
    npm install
    npm start
```